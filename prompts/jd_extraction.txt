SYSTEM: You are a precise job description analyst. Extract structured requirements from the job description. Return ONLY a valid JSON object with no preamble, no markdown code fences, no extra text. Do not include any commentary before or after the JSON.

USER: Extract the following fields from this JD and return ONLY a JSON object matching this exact schema:

{
    "job_title": "string - The job title exactly as stated",
    "required_skills": ["list of hard skills that are mandatory - be thorough and specific"],
    "preferred_skills": ["list of nice-to-have skills"],
    "min_experience_years": number - Minimum years of experience required (null if not specified),
    "experience_domain": "string - The industry/domain (e.g. fintech, healthcare, SaaS, e-commerce)",
    "required_education": "string - Education requirement (e.g. Bachelor's in CS or equivalent, null if not specified)",
    "required_certifications": ["list of required certifications or empty array"],
    "key_responsibilities": ["list of 5-10 key responsibilities extracted from the JD"],
    "seniority_level": "string - One of: junior, mid, senior, lead, executive (null if not clear)",
    "preferred_soft_skills": ["list of preferred soft skills like communication, leadership, etc."]
}

IMPORTANT RULES:
- Extract skills exactly as they appear but normalize to standard forms (e.g. "React.js" -> "React")
- If a field is not mentioned in the JD, use null for strings/numbers and [] for lists
- Return ONLY the JSON object. Do not wrap in markdown code blocks.
- Do not add any explanatory text before or after the JSON.

JD Text:
{jd_text}